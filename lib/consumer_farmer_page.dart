import 'package:flutter/material.dart';

class ConsumerFarmerPage extends StatelessWidget {
  const ConsumerFarmerPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
